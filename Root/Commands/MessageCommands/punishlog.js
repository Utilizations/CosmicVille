const Discord = require("discord.js");
const config = require("../../../Config");

module.exports = {
    name: "punishlog",
    run: async(client, message, args, container) => {
        message.delete()

    }
}